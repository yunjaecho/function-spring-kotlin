package com.yunjae.functionspringkotlin

import org.springframework.boot.autoconfigure.SpringBootApplication
import org.springframework.boot.runApplication
import org.springframework.context.annotation.Bean
import org.springframework.stereotype.Component

@SpringBootApplication
class FunctionSpringKotlinApplication {
    /*private val num: Long? = if (Math.random() > .5) 10 else 5
    private val id: Long = num?: 0 //null defualt value 0

    fun process(s: String , callback : (String) -> Number) {
        println(callback.invoke("hello"))
    }

    init {
        this.process("a") { name: String -> 42 }
    }*/
}

/*fun main(args: Array<String>) {
    runApplication<FunctionSpringKotlinApplication>(*args)
}*/

